# User-config overrides for the dongle central.
# This file is loaded after config/sofle.conf and overrides its BT connection limits.

# Dongle needs 5 connections: 2 peripherals + 3 BT host profiles
CONFIG_BT_MAX_CONN=5
CONFIG_BT_MAX_PAIRED=5
