# ── Output ──────────────────────────────────────────────────────────────────
# Dongle connects to the PC via USB.
CONFIG_ZMK_USB=y
# Disable BLE *host* advertising (ZMK_BLE controls BT_PERIPHERAL / output to
# computers & phones).  The BLE hardware and stack remain active because
# ZMK_SPLIT_BLE=y (see below) keeps BT_CENTRAL enabled so the dongle can
# still scan for and connect to the two Sofle keyboard halves.
CONFIG_ZMK_BLE=n

# ── Split ────────────────────────────────────────────────────────────────────
# Central role: connect to both Sofle halves as BLE peripherals.
CONFIG_ZMK_SPLIT=y
CONFIG_ZMK_SPLIT_BLE=y
CONFIG_ZMK_SPLIT_BLE_CENTRAL_PERIPHERALS=2

# Forward battery levels reported by the keyboard halves.
CONFIG_ZMK_SPLIT_BLE_CENTRAL_BATTERY_LEVEL_PROXY=y
CONFIG_ZMK_SPLIT_BLE_CENTRAL_BATTERY_LEVEL_FETCHING=y

# ── Features ─────────────────────────────────────────────────────────────────
# Keep mouse/pointing support in sync with the keyboard halves.
CONFIG_ZMK_POINTING=y

# ── Power ────────────────────────────────────────────────────────────────────
# Dongle is USB-powered; no need for sleep.
CONFIG_ZMK_SLEEP=n

# Raise TX power for reliable BLE split communication.
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

# ── BLE tweaks ───────────────────────────────────────────────────────────────
CONFIG_ZMK_BLE_EXPERIMENTAL_CONN=y
CONFIG_BT_GATT_ENFORCE_SUBSCRIPTION=n
